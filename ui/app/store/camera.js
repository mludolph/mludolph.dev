export const state = () => ({
  cameras: [],
  flipped: false,
  live: false,
  ready: false,
  selectedCameraId: null,
});

export const mutations = {};

export const actions = {};

export const getters = {};
