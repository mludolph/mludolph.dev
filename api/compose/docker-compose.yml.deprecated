version: '3.8'

services:
  api:
    container_name: cr.mludolph.dev/mludolph-api:latest
    networks:
      - external
      - backend

  redis:
    image: redis
    container_name: redis
    networks:
      - backend

networks:
  external:
    external:
      name: external
  backend:
    external:
      name: backend
